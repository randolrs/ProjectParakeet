// Drizzle schema. Intentionally empty in M0 — tables land in M1
// (users, company_preferences) and beyond. Keep this file as the single
// source of truth for the schema; migrations are generated from it.
export {};
